services:
  - type: web
    name: zavalaexpress-backend
    env: node
    plan: free
    branch: main
    rootDirectory: backend
    buildCommand: npm install --no-package-lock
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
